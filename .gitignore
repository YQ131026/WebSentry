# Ignore Python cache files
__pycache__/

# Ignore the actual config file (but keep the example)
src/config/config.py

# Ignore all files in the logs directory
logs/*

# Optional: keep the logs directory itself, but ignore its contents
!logs/.gitkeep
